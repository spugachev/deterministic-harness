{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "cargo fmt --all -- --check && cargo check --workspace 2>&1 | tail -20" }
        ]
      }
    ]
  }
}
